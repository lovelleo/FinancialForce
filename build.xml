<project name="Sample usage of Salesforce Ant tasks" default="retrieve" basedir="." xmlns:sf="antlib:com.salesforce">
    <property file="build.properties"/>
    <property environment="env"/>
    <condition property="sf.username" value=""> <not> <isset property="sf.username"/> </not> </condition>
    <condition property="sf.password" value=""> <not> <isset property="sf.password"/> </not> </condition>
    <condition property="sf.sessionId" value=""> <not> <isset property="sf.sessionId"/> </not> </condition>
    <taskdef resource="com/salesforce/antlib.xml" uri="antlib:com.salesforce">
        <classpath>
            <pathelement location="ant-salesforce.jar" />        	
        </classpath>
    </taskdef>
	<target name="retrieve">
	  <!-- Retrieve the contents into another directory -->
	  <sf:retrieve username="${sf.Cadenceusername}" password="${sf.Cadencepassword}" sessionId="${sf.sessionId}" serverurl="${sf.serverurl}" maxPoll="${sf.maxPoll}" retrieveTarget="Cadence 1" pollWaitMillis="40000" unpackaged="Cadence 1/package.xml"/>
    </target>
	<target name="push">
       <sf:deploy username="${sf.Cadenceusername}" password="${sf.Cadencepassword}" serverurl="${sf.serverurl}" maxPoll="${sf.maxPoll}" deployRoot="Cadence 1" rollbackOnError="true" />
    </target>
</project>
